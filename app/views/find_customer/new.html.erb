<h1>Procurar clientes</h1>
<%= form_tag(find_customer_index_path, :method => "get", :class => "simple_form") do  %>
  Nome:
  <%= text_field_tag "customer_name", nil,:class => 'customer_find' %>
  <p/>
  <%= submit_tag 'Procurar' %>

<% end %>
